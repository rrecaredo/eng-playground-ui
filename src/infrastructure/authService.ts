import { get } from "./httpService";

export const login = () => {
};

export const logout = () => {};

export const isAuthenticated = () => {
    return true;
};

export const getLoggedUser = () => {};
